; Used when PHP loads .user.ini from the document root (some local setups).
; Docker uses docker/php/custom.ini instead.
upload_max_filesize = 200M
post_max_size = 200M
max_execution_time = 600
max_input_time = 600
